{% load static %}
<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>Panel de Empleado | GiseNails</title>
    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <!-- Google Fonts - Inter -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet" />
    <!-- Estilos personalizados -->
    <link rel="stylesheet" href="{% static 'css/estilos_empleado.css' %}" />
    <!-- Iconos Bootstrap -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
  </head>
  <body>
    {% if user.is_authenticated %}
      <!-- Barra de navegación superior -->
      <nav class="navbar navbar-light bg-white border-bottom fixed-top shadow-sm" id="navbarEmpleado">
        <div class="container-fluid">
          <span class="navbar-brand fw-bold text-dark" style="font-size: 1.5rem;">Panel de Empleado</span>
          <div class="d-flex align-items-center gap-2 ms-auto">
            <button id="toggleTheme" class="btn btn-outline-secondary btn-sm d-flex align-items-center justify-content-center" style="width:38px;height:38px;padding:0;" title="Cambiar tema" aria-pressed="false">
              <i id="themeIcon" class="bi bi-moon-fill fs-5" aria-hidden="true"></i>
              <span class="visually-hidden">Cambiar tema</span>
            </button>

            <div class="dropdown">
              <a class="d-flex align-items-center text-decoration-none dropdown-toggle" href="#" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                <img src="{% static 'img/log.jpg' %}" alt="Avatar" width="40" height="40" class="rounded-circle border border-2 border-secondary shadow-sm" />
              </a>
              <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                <li>
                  <form action="{% url 'login:logout' %}" method="post" class="d-inline">
                    {% csrf_token %}
                    <button type="submit" class="dropdown-item"><i class="bi bi-box-arrow-right me-2"></i> Cerrar sesión</button>
                  </form>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </nav>

      <div style="height: 70px;"></div>

      <main class="container flex-grow-1">
        {% block content %}
        {% endblock %}
      </main>

      <footer class="text-center py-4" style="background: #f8fafc; color: #232c36; border-top: 1px solid #e5e7eb; box-shadow: 0 -2px 12px rgba(44,62,80,0.07); font-size: 1.05rem;">© 2025 GiseNails &nbsp;|&nbsp; Panel de Empleados</footer>
    {% else %}
      <main class="container my-5"></main>
    {% endif %}

    <!-- Bootstrap bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Cargar UNICO script para el tema -->
    <script src="{% static 'js/empleado_theme.js' %}"></script>

    {% block scripts %}
      <!-- Si otros templates necesitan añadir scripts, se añadirán aquí -->
    {% endblock %}
  </body>
</html>
